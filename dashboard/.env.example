# LibreView Credentials
LIBREVIEW_EMAIL=your-email@example.com
LIBREVIEW_PASSWORD='your-password-here'

# LibreView Follower Account (for realtime data)
LIBREVIEW_FOLLOWER_EMAIL=follower-email@example.com

# Archer Glucose Settings
LIBRE_OFFSET=3.2
TARGET_LOW=6.0
TARGET_HIGH=12.0

# Database Path (set by docker-compose)
# DB_PATH=/app/data/eyedrops.db
